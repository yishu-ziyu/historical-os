# Round 020 - Godot Demo Implementation

## Trigger

The user stopped the previous test story demo direction and handed off the real target project: an alternate-history simulation OS, with a prepared Godot demo prompt at `artifacts/godot_demo_prompt_v0.1.md`.

## Action

Implemented an isolated Godot project under:

```text
artifacts/godot_demo/
```

The implementation intentionally does not modify the Godot engine source under:

```text
/Users/mahaoxuan/Desktop/黑客松/游戏引擎/godot
```

## Implemented

- Top `HISTORICAL OS` status bar.
- Default intelligence workbench layout.
- Intel Desk main window.
- Timeline / Map / Archives / Risk windows.
- OS-style window title bars.
- Minimize/restore controls.
- Drag handling code for title bars.
- Five Intel Desk action buttons.
- Notification feedback area.
- Historical sensitivity notice.
- Data-driven text content in `data/workbench.json`.

## Verification

- Godot script check passed:

```bash
godot --headless --path artifacts/godot_demo --check-only --script res://scripts/Main.gd
```

- Web export succeeded:

```bash
godot --headless --path artifacts/godot_demo --export-release Web artifacts/godot_demo_web/index.html
```

- Browser console showed only Godot/WebGL/Emscripten initialization logs.
- Button click feedback was verified via Chrome DevTools canvas event dispatch.
- Screenshot evidence:

```text
artifacts/historical-os-demo-v0.1.png
artifacts/historical-os-demo-v0.1-click-test.png
artifacts/historical-os-demo-v0.1-drag-test.png
```

## Known Gap

Manual drag behavior needs user-side confirmation. The Godot drag code is present, but synthetic canvas drag through Chrome DevTools did not visibly move a window in the captured screenshot.

## Next

If the prototype direction is accepted, the next pass should focus on:

1. Making resize handles real, not just drag/minimize.
2. Improving window density and typography.
3. Adding a more convincing map panel.
4. Splitting the Intel Desk content into progressive task-flow states.
5. Adding source-verification and field-directive modal prototypes.
